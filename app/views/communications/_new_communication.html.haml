= form_for setup_communication(Communication.new), :url => {:controller => 'communications', :action => 'create', :format => :ajax}, :remote => true, :html => {'data-update-target' => 'communications_table'}, :method => 'POST' do |pl|
  %div.flex.three
    = pl.hidden_field :project_id, :value => project.id
    = render :partial => 'communications/communication', object: pl
    %div
      %label
      = submit_tag :Add
